- hosts:  localhost
  connection:  local
  become: true

  pre_tasks:
    - name:  update repositories
      dnf:
        update_cache:  yes
      changed_when:  Fasle

  tasks:
    - include:  tasks/packages.yml
